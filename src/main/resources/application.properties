# Application Name
spring.application.name=hamburger_restaurant

# Server Configuration
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/burger_restaurant
spring.datasource.username=root
spring.datasource.password=1903Besiktas1903
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Add these lines for better transaction management
spring.jpa.properties.hibernate.connection.isolation=2
spring.transaction.default-timeout=30
spring.jpa.properties.hibernate.connection.autocommit=true

# Logging Configuration
logging.level.com.example.hamburger_restaurant = DEBUG
logging.level.org.springframework.security = DEBUG

logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %5p [%t] - %c{1} - %m%n

server.servlet.session.timeout=1800

